/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'i.pravatar.cc',
      // otros dominios si los necesitas
    ],
  },
};

module.exports = nextConfig; 